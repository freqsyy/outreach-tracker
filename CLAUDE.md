# ⚡ ЖЕЛЕЗНЫЕ ПРАВИЛА СЕССИИ (грузятся автоматически в 100% окон)

Этот файл читается при КАЖДОМ старте Claude Code — любая папка, любое окно.
Не жди вопросов от Назара. Читай, применяй, работай.

---

## 👤 КТО ТАКОЙ НАЗАР
- 15 лет, Беларусь. QA-тестировщик (ручное тестирование сайтов) + учит английский.
- Ведёт «второй мозг» в Obsidian. Общается на русском, матом допустим (дружелюбно).
- Запрещён длинное тире «—» в текстах: только короткое «-» (см. память `dash-rules`).

## 🧠 ВТОРОЙ МОЗГ (Obsidian Vault) — ОБЯЗАТЕЛЬНО ПРИ СТАРТЕ
Путь: `C:\Users\nazar\Documents\Obsidian Vault\`
При старте сессии БЕЗ вопросов прочитать в этом порядке:
1. `wiki/index.md` — карта знаний, актуальные статусы проектов
2. `Проекты/` — статусы активных проектов (особенно «Аутрич тестировщиком»)
3. `Ежедневник/YYYY-MM-DD.md` — сегодняшний контекст (YYYY-MM-DD = сегодня)
4. `raw/index.md` — какие источники добавлены
Детальные правила работы с vault: см. `C:\Users\nazar\Documents\Obsidian Vault\CLAUDE.md`
(структура Raw→Wiki→Schema, операции Ingest/Query/Lint).

## 🛑 ЖЁСТКИЕ ЗАПРЕТЫ (НАРУШАТЬ = ПРОВАЛ)
- ❌ НЕ трогать `fcc-server.exe` и `C:\Users\nazar\.fcc\.env` — это рабочий прокси
  Назара на модели. НЕ останавливать, НЕ менять модели/ключи. (память `fcc-config-immutable`)
- ❌ НЕ убивать процессы без прямой команды (`taskkill`/`Stop-Process`/`pkill`).
- ❌ НЕ пушить в git без явного «да/пуш». Коммитить — можно, пушить — спрашивать.
- ❌ НЕ править `.fcc` никогда.
- ❌ НЕ трогать чужие секреты (`.env`, `bridge.env`, `*.env`) — держать вне git.

## 🛡️ ГОРДОН (главный проект, аутрич-бот)
Папка: `C:\Users\nazar\outreach-tracker\`
Суть: армия агентов ищет сайты, пишет разработчикам с предложением QA-тестировщика.
- `outreach.db` — БД (SQLite), 216+ сайтов. Статусы: `sent` / `pending` / `rejected` / `bounced` / `replied` / `hired` / `review` (review = авто-найденный скаутом, не готов к рассылке, нужен ручной аппрув). Это ИСТИННИК — беречь, бэкапить перед git-операциями.
- `audit_engine.py` — аудит сайта через Chromium CDP (порт 9222). Ранний выход после 1-го бага.
- `agent_auditor.py` / `audit_site.py` — агент-аудитор, пишет баг в `notes` (маркер `AUDIT::`) + `audits/<domain>.md`.
- `agent_sender.py` / `send_now.py` / `send_window.py` / `send_telegram.py` — отправка писем (Gmail, ротация 5 акков), форс-режим, оконный, ТГ-мост.
- `agent_parser.py` — парсинг сайтов из `gordon_sources.txt` → контакты в БД.
- `agent_scout.py` — АГЕНТ 0: ищет МОЛОДЫЕ сайты (не мега) через дамп WebsiteLaunches + фильтры RDAP-возраст/upvotes → в БД как `auto-scout,fresh`, статус `review` (НЕ шлётся без ручного аппрува). SECURITY: SSRF-гарды на URL/домен. Запуск: `python agent_scout.py [--days N --limit N --dry-run]`.
- `agent_recorder.py` — фиксация ответов (IMAP авто-поллинг ВКЛ) + ручной `gordon_responses.txt`. БАГ 2026-07-11: дубль `apply()` падал с TypeError — ПОЧИНЕНО (commit `b32a2dc`).
- `gordon_desktop.py` — дашборд (PyQt): живая лента + кнопки агентов (парсинг/ответы).
- `barter_gen.py` + `barter_posts.md` — авто-генерация бартер-текстов из багов (18 готовых).
- `bridge_*.py` + `gordon_bot_worker.js` — мост ПК↔Телеграм (Cloudflare Worker + KV).
- `sync.py` — синхронизация БД с GitHub (`pull`/`push`), `journal_mode=DELETE`.
**СТАТУС (конец 2026-07-11):** v0.4 + скаут + security-фиксы + письмо с CTA. Отправлено 103 письма, 78 pending в очереди (Назар велел СТОП). 14 коммитов ahead origin/main (Назар дал добро на пуш).
**АКТУАЛЬНЫЕ ЗАДАЧИ:** ПУШ в GitHub (добро есть) · дозасылка 78 pending · автопостинг бартера
(нужен Telegram-токен) · кнопка Scout в дашборд · #218-#222 (pending до фикса) → review.

## 🪟 МНОГООКОНОЧНОСТЬ (чтобы не было каши как 2026-07-11)
Если открыто НЕСКОЛЬКО окон Claude Code:
- Работай в РАЗНЫХ папках (разные проекты) ИЛИ
- Если в одной папке (outreach-tracker) — один писатель в git за раз, через `sync.py pull`/`push`.
- ❌ НЕ гонять аудит/рассылку из 2 окон одновременно — конфликт БД и git.

## 💾 АВТОСОХРАНЕНИЕ (без вопроса «сохранить?»)
Любой значимый результат → сразу в vault: `log.md` + `Ежедневник/СЕГОДНЯ` + `Проекты/<проект>`.
Создал скрипт/решил проблему/провёл сессию — запиши. Не копи к концу сессии. (память `auto-memory-rule`)

## 📌 ПРОЧИЕ ПРОЕКТЫ (кратко)
- **Джарвис** — голосовой ИИ-компаньон (дизайн утверждён, в разработке). Папка `C:\Users\nazar\jarvis`.
- **YouTube заработок** / **Обучение QA** / **Настройка второго мозга** — см. `Проекты/`.
- Полная карта: `C:\Users\nazar\Documents\Obsidian Vault\wiki\index.md`.
