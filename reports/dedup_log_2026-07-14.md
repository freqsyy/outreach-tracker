# Dedup Log — 2026-07-14 (dry-run, READ-ONLY)

- Источник: `dedup.py` (`is_already_contacted` + `dry_run_report`), интегрирован
  в `agent_scout.py` и `agent_parser.py` (задача `scout-2026-07-14-14`).
- Режим: **dry-run, БД НЕ тронута**. Показывает сколько дублей ОТСЕЯЛОСЬ БЫ.

## Сводка
- pending/review лидов в БД: **134**
- Закрытых статусов (sent/bounced/rejected): **187**
  - уникальных доменов в истории: 187
  - уникальных apex-семей: 187
  - уникальных email: 187
  - уникальных telegram: 150
- **ОТСЕЯЛОСЬ БЫ дублей: 0**

## Интерпретация
В текущей БД нет `pending`/`review` лидов, чей domain/apex/email/tg уже в закрытых
статусах - каждый лид уникален. Дедуп-фильтр работает НА ОПЕРЕЖЕНИЕ: при следующем
прогоне скаут/парсер, нашедший тот же домен второй раз (под другим URL/email/
поддоменом), будет отсеян ДО записи в БД. Это устраняет риск повторной рассылки
даже при гонке/падении прогона (раньше защита держалась только на `mark_sent()`).

## Защищённые крайние случаи
- Один домен — разные email -> отсекается по domain (компанию не спамим дважды).
- Поддомен vs apex (`app.x.com`/`x.com`) -> отсекается по apex-семье.
- www vs non-www -> нормализуется к одному домену.
- Разные URL, один домен -> все нормализуются к одному.

## ЗАПРЕТЫ (соблюдены)
БД не изменена (только read-only SELECT). Статусы существующих лидов НЕ менялись.
НЕ push / НЕ commit в чужое дерево / НЕ убивать процессы / НЕ fcc / НЕ vision. Тире только "-".
