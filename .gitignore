# Секреты - никогда не в git
.env
bridge.env
*.env

# Python
__pycache__/
*.pyc

# Скрап-дампы агента-парсера (тестовые HTML)
*.html

# Старые артефакты (не в публичный репо)
Наша_армия_агентов_итог.txt
gordon_bot.log
nul

# ВАЖНО: outreach.db, gordon_send_state.json, gordon_run.log, status.json
# НЕ игнорируются - GitHub Actions коммитит их обратно в репо.
# journal_mode=DELETE: WAL/SHM файлы не нужны (всё в outreach.db), но на всякий
# игнорим их, чтобы не попали в коммит при случайном переключении режима.
outreach.db-wal
outreach.db-shm

gordon_run.bat
gordon_task.xml

# Логи и временные артефакты агентов (не в репо)
*.log
*.out
__reply5.txt
kv_inbox_dump.txt
\!x.visible)
sites.db
bridge_*.txt
